# ============================================
# AI Personal Receptionist - line ending rules
# ============================================
# NOTE: git uses the LAST matching pattern, so the
# generic rule comes first and specific rules after.

* text=auto

*.sh  text eol=lf
*.conf text eol=lf
*.py  text eol=lf
*.md  text eol=lf
