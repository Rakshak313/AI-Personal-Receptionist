; ============================================================================
;  dialplan_from_internal.conf — minimal [from-internal] context
;  Append to /etc/asterisk/extensions.conf (or include it).
; ============================================================================

[from-internal]
exten => _X.,1,Dial(PJSIP/${EXTEN})
exten => _X.,n,Hangup()
